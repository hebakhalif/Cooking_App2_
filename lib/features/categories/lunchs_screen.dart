import 'package:flutter/material.dart';

class LunchsScreen extends StatelessWidget {
  const LunchsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}