import 'package:flutter/material.dart';

class GrocerisScreen extends StatelessWidget {
  const GrocerisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}